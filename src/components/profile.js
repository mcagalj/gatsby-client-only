import * as React from "react";

const Profile = () => (
  <div>
    <h3>Profile component</h3>
    <p>
      Renders only under path: <em>client-only-routes/profile</em>
    </p>
  </div>
);

export default Profile;
