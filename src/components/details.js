import * as React from "react";

const Details = () => (
  <div>
    <h3>Details component</h3>
    <p>
      Renders only under path: <em>client-only-routes/details</em>
    </p>
  </div>
);

export default Details;
